const API_ENDPOINT = '/users'
import { FETCH_RECORD } from '@/store/lib/actions'

export const state = () => ({
  users: {}
})

export const getters = {
  users: state => state.users
}

export const mutations = {
  
}

export const actions = {

}